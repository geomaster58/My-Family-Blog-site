<%= form_with(model: gram, local: true) do |form| %>
  <% if gram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gram.errors.count, "error") %> prohibited this gram from being saved:</h2>

      <ul>
      <% gram.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :header %>
    <%= form.text_field :header, id: :gram_header %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :gram_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
